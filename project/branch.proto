syntax = "proto3";

import "language.proto";

import "commit.proto";

package project;

message BranchMeta {
    repeated Language languages = 1;
}

message Branch {
    BranchMeta meta = 1;
    string name = 2;
    repeated Commit commits = 3;
}